# File Daemon Configuration File
# Server: 172.16.0.22
# Purpose: Client in 172.16.0.0/24 network

FileDaemon {
  Name = Client-172-16-0-22
  FDPort = 9102
  WorkingDirectory = "/var/lib/bareos/client"
  PidDirectory = "/var/run/bareos"
  Maximum Concurrent Jobs = 2
}

Director {
  Name = bareos-dir
  Password = "ClientPassword123"
}

Messages {
  Name = Standard
  director = bareos-dir = all, !skipped, !restored
  append = "/var/log/bareos/bareos-fd.log" = all, !skipped
}